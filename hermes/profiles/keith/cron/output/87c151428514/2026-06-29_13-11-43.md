# Cron Job: Polymarket news research agent

**Job ID:** 87c151428514
**Run Time:** 2026-06-29 13:11:43
**Schedule:** every 120m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script timed out after 120s: /root/.hermes/profiles/keith/scripts/polymarket_research_candidates.py
```

You are Keith's Polymarket read-only research agent. Use the script output as the ONLY starting point. The workflow is bet-first to conserve tokens: if the script says there are no candidates, write an IGNORE note and do not browse/search. If candidates are present, research only the listed candidate markets and their provided research_query values; do not scan broad news across unrelated genres. The candidate generator deliberately searches for tradable <=3-day markets across sports, politics/geopolitics, macro/regulatory, tech/AI/business, weather/events, and general news-driven markets. Crypto is intentionally excluded. Sports is capped and should not dominate when non-sports candidates exist. For each candidate you research, use accessible primary/official/reputable sources where possible, then write a concise note to /root/polymarket-intel/logs/research_notes.md and insert a row into the research_notes SQLite table if appropriate. You may recommend IGNORE, WATCH, or PAPER_BUY, but do not place live trades, do not request credentials, and do not use private keys. Only open/insert a paper trade if: the market resolves within the configured 3-day window, the token_id is exact, confidence is high, source URLs are included, and the reasoning explains why the news creates an edge not already obvious in price. Prefer WATCH over PAPER_BUY when uncertain. Final response should summarize candidates reviewed, sources used, and any paper-only action taken.

## Response

## Script Failure Report — polymarket_research_candidates.py

**Status:** TIMEOUT at 120s — no candidates produced.

### Root Cause

The Gamma API (Polymarket's public search) is partially degraded. Two of the four topic search queries are timing out:

| Query | Status |
|-------|--------|
| `trump`, `iran`, `fed` | ✅ 200 OK (~1.5s) |
| `openai`, `hurricane` | ❌ Timeout (>10s) |

The script makes up to 16 topic search calls (4 categories × 4 queries, each with a 20s timeout). Even a few slow calls easily blow past the 120s cron limit before the `public_search_candidates()` function completes. The local DB query (step 1) likely finished fine, but the script died in step 2 before `diversify()` or output.

### What Was Available in the DB

The local SQLite DB has 256 markets including several high-volume, near-resolution candidates for **today (June 29)** that resolve within the 3-day window:

- **Brazil vs Japan** — resolves June 29 17:00 UTC (~10h), $4.2M vol
- **Germany vs Paraguay** — resolves June 29 20:30 UTC, $1.5M vol
- **Netherlands vs Morocco** — resolves June 30 01:00 UTC, $1.4M vol

All are FIFA World Cup matches, all sports, all within the window. The script's diversification logic would have surfaced at most 2 of these (per the `MAX_PER_CATEGORY=2` sports cap).

### Recommendation

1. **Short-term:** Increase the script timeout from 120s to 300s so transient API slowness doesn't kill the run.
2. **Medium-term:** Add per-query timeouts tighter than 20s (e.g., 8s) and wrap `public_search_candidates` in a try/except so the script still emits whatever the local DB found even if the API flakes out.
3. **This run:** IGNORE — no script output means zero candidates to research per the bet-first policy. No web searches or browsing performed.

**[SILENT]**
